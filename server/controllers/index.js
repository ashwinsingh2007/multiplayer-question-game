const channelUsers = require("./channelUsers");
const channels = require("./channels");
const messages = require("./messages");
const users = require("./users");
module.exports = {
  channels,
  channelUsers,
  users,
  messages
};
